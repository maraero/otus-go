{
  "logger": {
    "level": "info",
    "outputPaths": ["stdout"],
    "errorOutputPaths": ["stderr"]
  },
  "server": {
    "host": "localhost",
    "httpPort": "8000",
    "grpcPort": "8001"
  },
  "storage": {
    "type": "in-memory",
    "driver": "postgres",
    "dsn": "postgresql://admin:admin@localhost:5432/calendar?sslmode=disable"
  }
}
